# Schema Notes

This folder will contain database setup scripts once the backend implementation begins.

For V1, the initial schema will support:

- Users
- Career paths
- Projects
- Modules
- Concepts
- Tasks
- User project enrollments
- User task progress

The first real database script should be added after the Spring Boot entities are finalized, so the schema and code stay aligned.

